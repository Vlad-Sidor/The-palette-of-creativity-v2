<li>
    <a className="header_adress" href="Mira Ave, 43, 212000, Mogilev, Republic of Belarus">Mira Ave, 43 RB</a>
 </li>

    <h3>info</h3>
    <h6>Some information about{price}</h6>
    <h6>Some different information{size}</h6>


    фильтр временно убран - нет атрибута в дата.жс

     capacity,
    price,
    minPrice,
    maxPrice,
    minSize,
    maxSize,
    breakfast,
    pets 
let people = getUnique(rooms, "capacity");
  people = people.map((item, index) => (
    <option key={index} value={item}>
      {item}
    </option>
  ));

     <div className="form-group">
          <label htmlFor="price">год{price}</label>
          <input
            type="range"
            name="price"
            min={minPrice}
            max={maxPrice}
            id="price"
            value={price}
            onChange={handleChange}
            className="form-control"
          />
        </div>

        <div class="our-team">
              <div class="picture">
                <img class="img-fluid" src={org4}/>
              </div>
              <div class="team-content">
                <h3 class="name">Четвертый организатор</h3>
                <h4 class="title">самый главный</h4>
              </div>
              <ul class="social">
               <p> Немного информации не повредит</p>
              </ul>
        </div>